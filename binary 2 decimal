//WAP to convert binary to decimal
#include <stdio.h>

int main(){
    int n=101;
    int bin=0;
    int power=1;
    
    while(n!=0){
        int r=n%10;
        n/=10;
        bin+=r*power;
        power*=2;
    }
    
    printf("%d",bin);
}
